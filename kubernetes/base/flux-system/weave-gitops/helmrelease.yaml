---
# yaml-language-server: $schema=https://k8s-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app weave-gitops
spec:
  chartRef:
    kind: OCIRepository
    name: *app
  interval: 1h
  values:
    additionalArgs:
      - --auth-methods=oidc
      - --oidc-secret-name=weave-gitops-oidc-auth
    adminUser:
      create: true
      createSecret: false
      username: admin
    image:
      repository: ghcr.io/gkalian/wego-app
      pullPolicy: IfNotPresent
      tag: v0.39.0-rc.3@sha256:abc892644b38f4b4a49f1f5dcaf8b15a219a3e6104a632bc9824b2f2365659e8
    ingress:
      enabled: false
    metrics:
      enabled: true
    networkPolicy:
      create: false
    podAnnotations:
      secret.reloader.stakater.com/reload: cluster-user-auth,weave-gitops-oidc-auth
    rbac:
      create: true
      viewSecretsResourceNames:
        - weave-gitops-oidc-auth
    resources:
      requests:
        cpu: 30m
        memory: 70Mi
      limits:
        memory: 210Mi
