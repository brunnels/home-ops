---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app weave-gitops
spec:
  chartRef:
    kind: OCIRepository
    name: *app
  interval: 1h
  values:
    additionalArgs:
      - --auth-methods=oidc
      - --oidc-secret-name=weave-gitops-oidc-auth
    adminUser:
      create: true
      createSecret: false
      username: admin
    image:
      repository: artifactory.devops.telekom.de/ghcr.io.docker/gkalian/wego-app
      pullPolicy: IfNotPresent
      tag: v0.39.0-rc.3
    ingress:
      enabled: false
    metrics:
      enabled: true
    networkPolicy:
      create: false
    podAnnotations:
      secret.reloader.stakater.com/reload: cluster-user-auth,weave-gitops-oidc-auth
    rbac:
      create: true
      viewSecretsResourceNames:
        - weave-gitops-oidc-auth
    resources:
      requests:
        cpu: 30m
        memory: 70Mi
      limits:
        memory: 210Mi
