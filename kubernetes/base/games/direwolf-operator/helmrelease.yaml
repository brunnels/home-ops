---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app direwolf-operator
spec:
  chartRef:
    kind: OCIRepository
    name: *app
  interval: 1h
  upgrade:
    crds: CreateReplace
    force: true
  values:
    service:
      app:
        annotations:
          io.cilium/lb-ipam-sharing-key: "{{ .Release.Namespace }}"
          metallb.universe.tf/allow-shared-ip: "{{ .Release.Namespace }}"
        controller: moonlight_proxy
        type: LoadBalancer

#    controllers:
#      direwolf_operator:
#        containers:
#          operator:
#            env:
#              WOLF_IMAGE: ghcr.io/games-on-whales/wolf:dev-moonlight-fixes
