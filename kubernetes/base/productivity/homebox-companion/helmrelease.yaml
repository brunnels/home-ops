---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: homebox-companion
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  interval: 1h
  values:
    defaultPodOptions:
      securityContext:
        fsGroup: 100
        fsGroupChangePolicy: OnRootMismatch
        runAsGroup: 100
        runAsNonRoot: true
        runAsUser: 1000
    controllers:
      homebox-companion:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/duelion/homebox-companion
              tag: 3.1.0@sha256:a255a940edc94db3442587b9f0304d31034d8ef265da6be0d5e8cdf7cf3a8464
            env:
              HBC_DISABLE_UPDATE_CHECK: true
              HBC_HOMEBOX_URL: http://homebox:8080
              HBC_IMAGE_QUALITY: medium
              HBC_LINK_BASE_URL: https://homebox.${SECRET_DOMAIN}
              HBC_LLM_ALLOW_UNSAFE_MODELS: true
              HBC_LLM_API_BASE: http://vllm-stack-router-service/v1
              HBC_LLM_API_KEY: "insecure"
              HBC_LLM_MODEL: hosted_vllm/google/gemma-3-1b-it
              HBC_LLM_TIMEOUT: 240
              HBC_LOG_LEVEL: DEBUG
              HBC_RATE_LIMIT_ENABLED: false
              HBC_SERVER_HOST: 0.0.0.0
              HBC_SERVER_PORT: &port 8000
              TZ: "${TIMEZONE}"
              UV_LINK_MODE: copy
            #envFrom:
            #  - secretRef:
            #      name: *app
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  failureThreshold: 3
                  httpGet:
                    path: /health
                    port: *port
                  initialDelaySeconds: 15
                  periodSeconds: 10
                  timeoutSeconds: 10
              readiness: *probes
            resources:
              requests:
                cpu: 10m
                memory: 512Mi
              limits:
                cpu: 1000m
                memory: 4Gi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities: { drop: ["ALL"] }
              readOnlyRootFilesystem: false
    persistence:
      cache:
        type: emptyDir
        globalMounts:
          - path: /home/appuser/.cache
    route:
      app:
        annotations:
          gethomepage.dev/description: AI Inventory Assistant
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Automation
          gethomepage.dev/icon: mdi-package-variant-closed-plus
          gethomepage.dev/name: Homebox Companion
        hostnames:
          - "homebox-companion.${SECRET_DOMAIN}"
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - identifier: app
    service:
      app:
        annotations:
          gatus.home-operations.com/endpoint: |
            group: internal
            alerts:
              - type: pushover
        ports:
          http:
            port: *port
