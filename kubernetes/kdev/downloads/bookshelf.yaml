---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app bookshelf
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  components:
    - ../../../components/pgo-db-init
    - ../../../components/theme-park
    - ../../../components/volsync
  dependsOn:
    - name: downloads-pgo-cluster
    - name: volsync
      namespace: storage
  interval: 1h
  path: ./kubernetes/base/downloads/bookshelf
  postBuild:
    substitute:
      APP: *app
      THEME_PARK_APP: readarr
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
